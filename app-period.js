/* Period Performance picker removed — the daily/weekly/monthly table now
   follows the date range above (see app-render1.js). Kept as a placeholder
   so the <script> tag in index.html stays valid. */
